# Task 6 — Required npm installs

## Frontend (run in `frontend/`)

```bash
npm install chart.js xlsx jspdf jspdf-autotable
npm install --save-dev @types/jspdf
```

## Backend (no new packages needed)
All required packages (sequelize, express, etc.) were installed in previous tasks.
